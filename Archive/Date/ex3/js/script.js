// два поля с type = date.
// дато начала занияй и дата финиша занятий
// выводим количество часов в этом диапазоне
'use strict'
window.onload = function () {
  but.onclick = function () {
    event.preventDefault();
    var start = new Date();
    var end = new Date(dateEnd.value);
    console.log(dateStart.value);
    console.log(start);

    document.cookie = "name=privet; expires=";
  }
}
